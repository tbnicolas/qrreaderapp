import 'package:flutter/material.dart';

class DireccionesPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text('Direcciones Page'),
    );
  }
}